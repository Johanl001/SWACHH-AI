# Run this to create actual passwords:
# docker exec -it mosquitto mosquitto_passwd -c /mosquitto/config/passwd gateway_user
# docker exec -it mosquitto mosquitto_passwd /mosquitto/config/passwd dashboard_user
#
# Example users needed:
# - gateway_user  (used by Raspberry Pi edge_ai and gateway services)
# - dashboard_user (used by admin dashboard browser client)
